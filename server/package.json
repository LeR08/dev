{
  "name": "tally-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal backend for Tally: holds Stripe/PayPal secret keys and tracks freemium subscription status. Never bundled into the app.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "stripe": "^17.5.0"
  }
}
